export enum FormatDistanceType {
  Kilometers = 1000,
  Meters = 1,
}

export enum FormatRadiusType {
  Traininghistory = 50,
}

export enum FormatDifferenceType {
  Arcs = 4,
  SwooshesOuter =  5,
  SwooshesInner = 1.5,
}

export enum FormatDate {
  Day = 'day',
  Week = 'week',
  Month = 'month',
  Year = 'year',
}
